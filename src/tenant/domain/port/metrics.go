package port

// Metric event name constants for the tenant domain (ADR-002).
const (
	MetricTenantCreated = "tenant.created"
	MetricTenantDeleted = "tenant.deleted"
	MetricTenantPlanSet = "tenant.plan_changed"
)
